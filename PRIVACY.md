# プライバシーポリシー

Kawawa (以下「本拡張機能」) は、ユーザーのプライバシーを最大限に尊重します。本ポリシーは、本拡張機能がどのような情報を取り扱うかを説明するものです。

## 結論

**本拡張機能は、いかなる個人情報も、利用情報も、一切収集・保存・送信しません。**

## 取得する情報

ありません。

本拡張機能は、対象ページ (x.com / twitter.com) に CSS を注入してフォント表示を変更する機能のみを提供します。以下のいずれも行いません。

- ページ内容 (投稿、メッセージ、プロフィール等) の読み取り
- 入力内容 (投稿欄、検索欄、ログイン情報等) の読み取り
- 閲覧履歴・操作履歴の記録
- Cookie や認証トークンの読み取り
- ユーザー識別子・端末識別子の取得

## 外部通信

ありません。

本拡張機能は外部サーバーとの通信を一切行いません。Google Fonts などの Web フォントも読み込みません。すべての処理はユーザーのブラウザ内で完結します。

## ローカル保存

ありません。

本拡張機能は `chrome.storage` / `localStorage` / `IndexedDB` / `Cookie` のいずれにも書き込みを行いません。

## 第三者への提供

該当なし (そもそも情報を取得していないため)。

## 権限について

本拡張機能の `manifest.json` で要求している権限は以下の通りです。

- `content_scripts.matches`: `https://x.com/*`, `https://twitter.com/*`
  - 上記サイトに CSS を注入するためにのみ使用します
- `permissions`: なし
- `host_permissions`: なし

`tabs`, `storage`, `webRequest`, `cookies` などの権限は一切要求しません。

## 子どものプライバシー

本拡張機能は年齢を問わず安全に利用できます。子どもから情報を収集することもありません。

## ポリシーの変更

本ポリシーを変更する場合、本ファイルを更新します。重要な変更がある場合はリリースノート等で告知します。

## お問い合わせ

本ポリシーに関するご質問は、本リポジトリの GitHub Issues までお寄せください。

## 最終更新日

2026-05-05
